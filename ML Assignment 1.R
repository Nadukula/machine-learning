View(Rainfall_data)  
attach(Rainfall_data)# to display the variables
head(Rainfall_data)# to get the first five columns
tail(Rainfall_data) # to get the last five columns
summary(Rainfall_data)# to get median,mean and max values

plot(Specific.Humidity,Temperature,main = scatterplot)# to represent the scatter plot by using plot command
